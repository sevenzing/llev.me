1. looks like width of section description is different for the network and keys & signature. I like more the network size, make everything in one standard

2. in mempool section, in hint add "Also try to send invalid tx and see that miner simply ignores this tx". also add message that "there is blockchain at bottom, see produced transaction there!" you can change wording if you want. dont forget about translation


3. 
in final playground:
* "until you propagate the changes!" -- change "until you propagate the changes by pressing `sync network` button"
* "NETWORK OUT OF SYNC" -- make more visible
* add more examples what you can do in this playground in hint section